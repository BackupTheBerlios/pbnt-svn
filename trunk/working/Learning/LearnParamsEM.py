def learnParamsEM( engine, trials, iterations ):
        
        #initialize all of the nodes' CPTs
        for node in engine.bnet.nodes:
                node.CPT.CPT = 
        for i in range( iterations ):
                